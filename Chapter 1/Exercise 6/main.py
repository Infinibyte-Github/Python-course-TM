# Get hours, minutes and seconds from user
hours = int(input("Hours: "))
minutes = int(input("Minutes: "))
seconds = float(input("Seconds: "))

# Print time
print("Time is " + str(hours) + " hours, " + str(minutes) + " minutes and " + str(seconds) + " seconds")